# Credentials — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo

# Large databases
RAG.db
*.lock
transfer/neo4j.dump

# Python virtual environment
.venv/

# Large generated data files — reproduce with:
#   scripts/extract_vectors.py       (WSL2, one-time)
#   scripts/precompute_embeddings.py (GPU)
data/vectors.npy
data/vector_paperids.json
data/paper_embeddings.npy
data/paper_ids.json
data/_extract_checkpoint.json

# Evaluation cache — auto-regenerated on eval runs
results/eval_cache/

# PDF corpus — not redistributed
semantic_scholar_corpus/pdfs/

# OS / editor
.DS_Store
.claude/
.ipynb_checkpoints/

# Ingestion checkpoint — machine-specific state
ingestion/ingestion_state.json
